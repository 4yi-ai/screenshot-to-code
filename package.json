{
  "name": "screenshot-to-code",
  "description": "Turn any UI screenshot into clean, self-contained HTML + Tailwind in seconds — powered by Claude vision. Prototype faster, rebuild any design, and go from image to working code in one step.",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "pnpm run test:frontend && pnpm run test:backend",
    "test:frontend": "pnpm -C frontend test",
    "test:backend": "cd backend && poetry run pytest"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "packageManager": "pnpm@10.32.1"
}
